<a href="/user">HOME</a>
<% movies.forEach(movie => { %>
    <h1>
        <a href="/detail/<%= movie.imdbID %>">
             <%= movie.title %>  
        </a>
    </h1>
<dvi>
    <ul>
        <img src="<%= movie.poster %>"  alt="poster">
    </ul>
</dvi>    
<% }) %>